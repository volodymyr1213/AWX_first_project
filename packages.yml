---
- name: Installation pckg
  hosts:
  become:
  tasks:
    - name: install basic packages
      action: >
        {{ ansible_pkg_mgr }} name=vim state=present update_cache=yes
    with_items:
        - vim
        - telnet
        - tree